version: '3'
services:
        web:
                image: flask_petite_pandas # Change the image name to something unique
                build: .
                ports:
                        - "1234:8080" # Edit the number on the left to match the port you chose
                volumes:
                        - ./volumes:/volumes
                        - ./instance:/instance
                restart: unless-stopped